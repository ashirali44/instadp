import { Repository } from '../core/repository';
export declare class ClipsRepository extends Repository {
    nextMaxId: string;
    moreAvailable: boolean;
    set state(body: any);
    discover(module: string | "clips_viewer_clips_tab", seen: [] | []): Promise<any>;
    seen(reels: [] | any): Promise<any>;
}
